#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
🔐 محلل قوة كلمة المرور - Password Strength Analyzer
👨‍💻 المطور: grob 7rb
🚀 الإصدار: 1.0.0
📧 للاستخدام التعليمي فقط
"""

import os
import sys
import re
import string
import random
import time
from datetime import datetime

def مسح_الشاشة():
    """مسح شاشة الأوامر"""
    os.system('cls' if os.name == 'nt' else 'clear')

class محلل_كلمة_المرور:
    def __init__(self):
        self.المطور = "grob 7rb"
        self.الإصدار = "v1.0.0"
        self.مسار_البرنامج = os.path.dirname(os.path.abspath(sys.argv[0]))
        
    def عرض_القائمة(self):
        """عرض القائمة الرئيسية"""
        مسح_الشاشة()
        
        # ترويسة البرنامج
        print("="*60)
        print(f"{' '*18}🔐 محلل كلمة المرور")
        print("="*60)
        print(f"{' '*20}المطور: {self.المطور}")
        print(f"{' '*20}الإصدار: {self.الإصدار}")
        print("="*60)
        print()
        
        # القائمة
        print("╔══════════════════════════════════════════════════════╗")
        القائمة = [
            "<@1> تحليل كلمة مرور      <@8> معلومات الأداة",
            "<@2> إنشاء كلمة قوية      <@9> تحديث الأداة",
            "<@3> تحليل مجموعة        <10> المساعدة",
            "<@4> نصائح أمنية         <11> توثيق الحساب",
            "<@5> اختبار السرعة       <12> الإحصائيات",
            "<@6> تصدير التقارير      <13> إعادة التعيين",
            "<@7> الإعدادات           <14> الخروج"
        ]
        
        for سطر in القائمة:
            print(f"║ {سطر:<52} ║")
        
        print("╚══════════════════════════════════════════════════════╝")
        print(f"\n{' '*22}⚡ {self.الإصدار}")
        print("="*60)
    
    def تحليل_مفصل(self, كلمة_المرور):
        """تحليل مفصل لكلمة المرور"""
        نقاط = 0
        تقرير = []
        
        # فحص الطول
        الطول = len(كلمة_المرور)
        if الطول >= 16:
            نقاط += 4
            تقرير.append("✓ طول ممتاز (16+ حرف)")
        elif الطول >= 12:
            نقاط += 3
            تقرير.append("✓ طول جيد (12+ حرف)")
        elif الطول >= 8:
            نقاط += 2
            تقرير.append("✓ طول مقبول (8+ حرف)")
        else:
            تقرير.append("✗ قصير جداً (<8 أحرف)")
        
        # فحص تنوع الأحرف
        أنواع = {
            'أحرف كبيرة': any(c.isupper() for c in كلمة_المرور),
            'أحرف صغيرة': any(c.islower() for c in كلمة_المرور),
            'أرقام': any(c.isdigit() for c in كلمة_المرور),
            'رموز خاصة': any(c in string.punctuation for c in كلمة_المرور)
        }
        
        for النوع, موجود in أنواع.items():
            if موجود:
                نقاط += 1
                تقرير.append(f"✓ تحتوي على {النوع}")
            else:
                تقرير.append(f"✗ تفتقد {النوع}")
        
        # فحص التكرار
        إذا كان len(set(كلمة_المرور)) < len(كلمة_المرور) * 0.6:
            نقاط -= 1
            تقرير.append("⚠ يوجد أحرف مكررة كثيرة")
        
        return نقاط, تقرير
    
    def تشغيل_التحليل(self):
        """تشغيل أداة التحليل"""
        مسح_الشاشة()
        print("\n" + "="*60)
        print("🔍 تحليل قوة كلمة المرور")
        print("="*60)
        
        كلمة_المرور = input("\n🔐 أدخل كلمة المرور: ")
        
        if not كلمة_المرور:
            print("\n❌ لم تدخل كلمة مرور!")
            time.sleep(1)
            return
        
        print(f"\n{' '*10}⚙️  جاري التحليل...")
        time.sleep(0.5)
        
        نقاط, تقرير = self.تحليل_مفصل(كلمة_المرور)
        
        مسح_الشاشة()
        print("\n" + "="*60)
        print("📊 نتائج التحليل")
        print("="*60)
        
        print(f"\n📏 الطول: {len(كلمة_المرور)} حرف")
        print(f"🎯 النتيجة: {نقاط}/10")
        
        # شريط التقدم
        شريط = '█' * min(نقاط, 10) + '░' * (10 - min(نقاط, 10))
        print(f"\n📈 مستوى الأمان: [{شريط}]")
        
        print("\n📋 التفاصيل:")
        for عنصر in تقرير[:8]:  # عرض أول 8 عناصر فقط
            print(f"  {عنصر}")
        
        # التوصية
        print(f"\n💡 التوصية: ", end="")
        إذا كان نقاط >= 9:
            print("ممتازة! 👑")
        elif نقاط >= 7:
            print("جيدة جداً 👍")
        elif نقاط >= 5:
            print("مقبولة ⚠️")
        else:
            print("ضعيفة، يجب تغييرها ❌")
        
        input(f"\n↵ اضغط Enter للعودة...")
    
    def تشغيل_المنشئ(self):
        """تشغيل منشئ كلمات المرور"""
        مسح_الشاشة()
        print("\n" + "="*60)
        print("🎯 منشئ كلمات المرور القوية")
        print("="*60)
        
        try:
            الطول = input("\n📏 أدخل الطول المطلوب (8-64): ").strip()
            الطول = int(الطول) if الطول.isdigit() and 8 <= int(الطول) <= 64 else 16
            
            print("\n🎨 أنواع الأحرف:")
            print("  1. أحرف كبيرة وصغيرة + أرقام")
            print("  2. جميع الأحرف + رموز خاصة")
            
            النوع = input("\n🔧 اختر النوع (1 أو 2): ").strip()
            
            # إنشاء كلمة المرور
            إذا كان النوع == "1":
                أحرف = string.ascii_letters + string.digits
            else:
                أحرف = string.ascii_letters + string.digits + string.punctuation
            
            كلمة_مرور = ''.join(random.choice(أحرف) for _ in range(الطول))
            
            مسح_الشاشة()
            print("\n" + "="*60)
            print("✅ كلمة المرور المُنشأة")
            print("="*60)
            
            print(f"\n🔐 {كلمة_مرور}")
            print(f"\n📊 التفاصيل:")
            print(f"  • الطول: {الطول} حرف")
            print(f"  • القوة: {sum(1 for c in كلمة_مرور if c in string.ascii_letters + string.digits + string.punctuation)}/10")
            
            print(f"\n💡 نصيحة: احفظها في مدير كلمات مرور آمن")
            
        except ValueError:
            print("\n❌ قيمة غير صالحة!")
        
        input(f"\n↵ اضغط Enter للعودة...")
    
    def تشغيل_النصائح(self):
        """عرض النصائح الأمنية"""
        مسح_الشاشة()
        print("\n" + "="*60)
        print("🔒 نصائح أمنية مهمة")
        print("="*60)
        
        النصائح = [
            "🔐 استخدم طول 12 حرف على الأقل",
            "🎨 اجمع بين أنواع مختلفة من الأحرف",
            "🚫 لا تستخدم معلومات شخصية",
            "🔄 غيّر كلمات المرور بانتظام",
            "📱 استخدم مدير كلمات مرور",
            "🔐 فعّل المصادقة الثنائية (2FA)",
            "⚠️  احذر من التصيد الاحتيالي",
            "🔍 تأكد من أمان المواقع"
        ]
        
        print()
        for i, نصيحة in enumerate(النصائح, 1):
            print(f"  {i}. {نصيحة}")
            time.sleep(0.2)
        
        input(f"\n↵ اضغط Enter للعودة...")
    
    def تشغيل_المعلومات(self):
        """عرض معلومات الأداة"""
        مسح_الشاشة()
        print("\n" + "="*60)
        print("ℹ️  معلومات عن الأداة")
        print("="*60)
        
        print(f"""
👨‍💻 المطور: {self.المطور}
🚀 الإصدار: {self.الإصدار}
📅 تاريخ البناء: {datetime.now().strftime('%Y-%m-%d')}

🎯 الغرض:
  هذه الأداة للاستخدام التعليمي فقط
  تهدف لزيادة الوعي بأهمية كلمات المرور الآمنة

📋 المميزات:
  • تحليل مفصل لقوة كلمات المرور
  • إنشاء كلمات مرور قوية عشوائياً
  • نصائح أمنية متقدمة
  • واجهة عربية كاملة

⚠️  تحذير:
  لا تستخدم هذه الأداة لأي غرض غير قانوني
  المطور غير مسؤول عن سوء الاستخدام
""")
        
        input(f"\n↵ اضغط Enter للعودة...")
    
    def الخروج(self):
        """إنهاء البرنامج"""
        مسح_الشاشة()
        print("\n" + "="*60)
        print("👋 شكراً لاستخدامك الأداة")
        print("="*60)
        print(f"\n{' '*15}✨ {self.المطور}")
        print(f"{' '*15}🚀 {self.الإصدار}")
        print(f"\n{' '*10}للتواصل: للأغراض التعليمية فقط")
        print("\n" + "="*60)
        time.sleep(2)
        sys.exit(0)
    
    def تشغيل(self):
        """الدالة الرئيسية لتشغيل البرنامج"""
        while True:
            self.عرض_القائمة()
            
            try:
                اختيار = input("\n🔧 اختر رقم الوظيفة (1-14): ").strip()
                
                if اختيار == "1":
                    self.تشغيل_التحليل()
                elif اختيار == "2":
                    self.تشغيل_المنشئ()
                elif اختيار == "3":
                    print("\n🔧 هذه الميزة قيد التطوير...")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "4":
                    self.تشغيل_النصائح()
                elif اختيار == "5":
                    print("\n🔧 هذه الميزة قيد التطوير...")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "6":
                    print("\n🔧 هذه الميزة قيد التطوير...")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "7":
                    print("\n🔧 هذه الميزة قيد التطوير...")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "8":
                    self.تشغيل_المعلومات()
                elif اختيار == "9":
                    print("\n🔍 جاري البحث عن تحديثات...")
                    print("✅ أنت تستخدم أحدث إصدار")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "10":
                    print("\n📚 تعليمات الاستخدام:")
                    print("  • اختر رقم الوظيفة المطلوبة")
                    print("  • اتبع التعليمات على الشاشة")
                    print("  • استخدم كلمات مرور تجريبية للاختبار")
                    input("\n↵ اضغط Enter للعودة...")
                elif اختيار == "11":
                    print("\n🔐 هذه ميزة تجريبية")
                    print("   قيد التطوير حالياً")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "12":
                    print("\n📊 إحصائيات البرنامج:")
                    print("  • عدد الوظائف: 14")
                    print("  • اللغة: العربية")
                    print("  • الحالة: نشط")
                    print("  • الدعم: تعليمي فقط")
                    input("↵ اضغط Enter للعودة...")
                elif اختيار == "13":
                    print("\n🔄 جاري إعادة التعيين...")
                    time.sleep(1)
                    print("✅ تمت العملية بنجاح")
                    input("↵ اضغط Enter للمتابعة...")
                elif اختيار == "14":
                    self.الخروج()
                else:
                    print("❌ رقم غير صحيح!")
                    time.sleep(1)
                    
            except KeyboardInterrupt:
                self.الخروج()
            except Exception as e:
                print(f"\n❌ حدث خطأ: {e}")
                time.sleep(2)

# نقطة الدخول الرئيسية
if __name__ == "__main__":
    try:
        # تهيئة البرنامج
        برنامج = محلل_كلمة_المرور()
        
        # عرض رسالة ترحيب
        مسح_الشاشة()
        print("\n" + "="*60)
        print(f"{' '*15}🚀 تشغيل محلل كلمة المرور")
        print("="*60)
        print(f"{' '*10}⚙️  جاري تهيئة البرنامج...")
        time.sleep(1)
        
        # بدء التشغيل
        برنامج.تشغيل()
        
    except KeyboardInterrupt:
        print("\n\n⚠ تم إيقاف البرنامج")
        sys.exit(0)
    except Exception as e:
        print(f"\n❌ خطأ غير متوقع: {e}")
        input("\n↵ اضغط Enter للخروج...")
        sys.exit(1)